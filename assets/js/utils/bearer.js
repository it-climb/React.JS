'use strict';

/**
 * We have removed it since we are going to use only cookie auth mechanism.
 */
export default function (request) {
    // let token = cookie.load("access_token");
    // if (token) {
    //     request.set('Authorization', 'Bearer ' + token);
    // }
}